extends ./layout.pug
block content
    h1= title
    form(method="post")
        | 邮箱
        input(name="email" id="emailInput")
        br
        | 密码：
        input(name="password" id="passwordInput")
        br
        button(type="button" id="loginBtn") 登录
    script(src="https://unpkg.com/axios/dist/axios.min.js")
    script(src="/js/login.js")
